<Application x:Class="PhotoSorterApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/Colors.Light.xaml"/>
                <ResourceDictionary Source="/Themes/Brushes.xaml"/>
                <ResourceDictionary Source="/Styles/Animations.xaml"/>
                <ResourceDictionary Source="/Styles/Controls.xaml"/>
                <ResourceDictionary Source="/Styles/Typography.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Base font -->
            <FontFamily x:Key="AppFontFamily">Segoe UI</FontFamily>

            <!-- Map SystemColors keys to theme tokens so Menu popups and other system-drawn controls follow theme -->
            <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="{DynamicResource SurfaceColor}"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="{DynamicResource TextColor}"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="{DynamicResource SurfaceColor}"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="{DynamicResource TextColor}"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="{DynamicResource PrimaryColor}"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="{DynamicResource TextOnPrimaryColor}"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="{DynamicResource BackgroundColor}"/>

            <!-- Global Window style uses DynamicResource so theme can switch at runtime -->
            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            </Style>
            <Style TargetType="Control">
                <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}"/>
                <Setter Property="FontSize" Value="14"/>
            </Style>

            <!-- Default text styles and inputs are defined in Styles/Controls.xaml to keep resource organization consistent -->

        </ResourceDictionary>
    </Application.Resources>
</Application>