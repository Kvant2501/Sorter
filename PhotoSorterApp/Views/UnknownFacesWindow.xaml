<Window x:Class="PhotoSorterApp.Views.UnknownFacesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:resources="clr-namespace:PhotoSorterApp.Resources"
        Title="{x:Static resources:UiStrings.UnknownFacesWindow_Title}"
        Height="620" Width="1080" ResizeMode="CanResize"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Card}" Margin="0,0,0,10">
            <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource SubtleTextBrush}" Text="{x:Static resources:UiStrings.UnknownFacesWindow_HeaderDescription}" />
        </Border>

        <Grid Grid.Row="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
                <ProgressBar IsIndeterminate="True" Width="300" Height="6" Margin="0,0,0,12"/>
                <TextBlock HorizontalAlignment="Center" FontSize="14"
                           Foreground="{DynamicResource SubtleTextBrush}" Text="{x:Static resources:UiStrings.UnknownFacesWindow_Loading}" />
            </StackPanel>

            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibility}}">
                <ItemsControl ItemsSource="{Binding GroupedItems}">

                    <ItemsControl.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0,0,0,1"
                                            Margin="0,10,0,4" Padding="4,4,0,4">
                                        <TextBlock FontWeight="Bold" FontSize="13"
                                                   Foreground="{DynamicResource AccentBrush}">
                                            <TextBlock.Text>
                                                <MultiBinding StringFormat="{}{0} ({1})">
                                                    <Binding Path="Name"/>
                                                    <Binding Path="ItemCount"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                            <GroupStyle.ContainerStyle>
                                <Style TargetType="GroupItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="GroupItem">
                                                <StackPanel>
                                                    <ContentPresenter/>
                                                    <ItemsPresenter/>
                                                </StackPanel>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </GroupStyle.ContainerStyle>
                        </GroupStyle>
                    </ItemsControl.GroupStyle>

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="108" Height="108" Margin="4"
                                    CornerRadius="6" BorderThickness="2"
                                    Cursor="Hand">
                                <Border.ToolTip>
                                    <ToolTip>
                                        <TextBlock Text="{Binding FileName}"/>
                                    </ToolTip>
                                </Border.ToolTip>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftClick"
                                                  Command="{Binding ToggleSelectedCommand}"/>
                                </Border.InputBindings>
                                <Grid ClipToBounds="True">
                                    <Image Source="{Binding Preview}" Stretch="UniformToFill"
                                           Width="104" Height="104"
                                           VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    <Border CornerRadius="4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Background" Value="#440078D4"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <Border Width="22" Height="22" CornerRadius="11"
                                            HorizontalAlignment="Right" VerticalAlignment="Top"
                                            Margin="0,4,4,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="&#10003;" FontSize="13" FontWeight="Bold"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Foreground="White">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock VerticalAlignment="Center" Margin="0,0,16,0">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0}: {1}">
                        <Binding Source="{x:Static resources:UiStrings.UnknownFacesWindow_FoundSuffix}"/>
                        <Binding Path="Items.Count"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <Button Style="{StaticResource SecondaryButton}" Content="{x:Static resources:UiStrings.UnknownFacesWindow_ClearSelection}" Click="ClearSelection_Click" Width="140" Margin="0,0,8,0"/>
            <Button Style="{StaticResource SecondaryButton}" Content="{x:Static resources:UiStrings.UnknownFacesWindow_MoveToCluster}" Click="MoveToCluster_Click" Width="170" Margin="0,0,8,0"/>
            <Button Style="{StaticResource DangerButton}" Content="{x:Static resources:UiStrings.UnknownFacesWindow_DeleteSelected}" Click="DeleteSelected_Click" Width="160" Margin="0,0,8,0"/>
            <Button Style="{StaticResource PrimaryButton}" Content="{x:Static resources:UiStrings.UnknownFacesWindow_AssignNameToClusters}" Click="AssignSelected_Click" Width="190" Margin="0,0,8,0" ToolTip="{x:Static resources:UiStrings.UnknownFacesWindow_AssignTooltip}"/>
            <Button Style="{StaticResource SecondaryButton}" Content="{x:Static resources:UiStrings.UnknownFacesWindow_Close}" Click="Close_Click" Width="90"/>
        </StackPanel>
    </Grid>
</Window>
